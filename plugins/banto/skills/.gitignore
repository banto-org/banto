
# === AI Context (managed by banto) ===
# in-repo .ai-context/ layout は 5.30 で廃止（現行: 中央 store ~/ai-context-store/<project>/）。
# 未移行 repo の grandfather 用に legacy ディレクトリ全体を ignore する
# （scaffold が store へ非破壊コピーで自動移行する — decision 2026-07-08 abolish-in-repo）。
.ai-context/
# === /AI Context ===
