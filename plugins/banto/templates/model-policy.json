{
  "schema_version": 2,
  "_note": "Operational model defaults only (single source of truth). The prescriptive fan-out roles (design/implement/audit/audit_alt/mechanical) were removed on 2026-07-30 (decision 2026-07-30-000956): fan-out model selection is delegated entirely to the main AI, and rules prescribe only granularity and parallelism (templates/rules/quality.md). The 12-cell measurement remains recorded as evidence in decisions/2026-07-02-100946 but is no longer used as a prescriptive default. summarize=sonnet for faithful transcript->template summarization (the idle-checkpoint background fork): the cost motive rules out opus, and sonnet is measured to pass where haiku is the sole miss. verify_external is a different-vendor judge for cross-check.sh (decisions/2026-07-10-070500): a separate provider mitigates self-preference bias, and multi-agent deliberation is deliberately not adopted (it can amplify bias), so this stays a single-shot refutation review. cost_optimized_nda_ok=false because DeepSeek documents in-China data processing, which the PII protection rule forbids for NDA engagements. Owner decision (2026-07-10): OpenRouter is not adopted as a paid contract, so verify_external prioritizes each user's own subscription CLI (codex, then gemini) and keeps openrouter as an API-key fallback only; claude is excluded from auto-detection as a same-vendor judge (self-preference bias) and requires explicit opt-in.",
  "roles": {
    "summarize": "sonnet",
    "verify_external": {
      "backends": ["codex", "gemini", "openrouter"],
      "same_vendor_backend": "claude",
      "openrouter": {
        "default": "x-ai/grok-build-0.1",
        "cost_optimized": "deepseek/deepseek-v4-flash",
        "cost_optimized_nda_ok": false
      }
    }
  }
}
