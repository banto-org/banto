# ODD Spec for memo
# schema: ../../templates/odd/odd.schema.yaml

schema_version: 1
skill: memo
autonomy_level: L1  # lightweight utility (saving [Memo] documents)

in_scope:
  - "Turning conversation content into [Memo]-prefixed documents ({base}/docs/)"

out_of_scope:
  - Recording design decisions (decisions/ — ai-context skill)
  - Status reports (status skill)

safety_boundaries:
  - Write only to new [Memo] files under {base}/docs/

kill_switch_conditions: []
approval_gates: []
