---
paths:
  - "**/*.test.*"
  - "**/*.spec.*"
  - "**/tests/**"
  - "**/__tests__/**"
  - "**/test_*"
---

# テストルール

- テストは実際の挙動を検証する。実装の詳細をテストしない
- テスト名は「何が」「何をすべきか」を明確に述べる
- モックは最小限に — 外部依存のみ
- テスト 1 つにつきアサーション 1 つ（複雑なケースは除く）
- テストデータはテスト内で自己完結させる。グローバル状態に依存しない
