# ODD Spec for memo
# schema: ../../templates/odd/odd.schema.yaml

schema_version: 1
skill: memo
autonomy_level: L1  # 軽量ユーティリティ（[Memo] ドキュメントの保存）

in_scope:
  - "会話内容の [Memo] プレフィックス付きドキュメント化（{base}/docs/）"

out_of_scope:
  - 設計判断の記録（decisions/ — ai-context skill）
  - ステータス報告（status skill）

safety_boundaries:
  - Write は {base}/docs/ 配下の新規 [Memo] ファイルのみ

kill_switch_conditions: []
approval_gates: []
